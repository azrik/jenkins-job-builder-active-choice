- job:
    name: 'cascade-choice-example-02'

    parameters:
      - uno-cascade-choice:
          name: CASCADE_CHOICE
          project: 'cascade-choice-example-02'
          script: |
            return ['foo', 'bar']
